import { CrossBrowserResult } from './cross-browser-result';

describe('CrossBrowserResult', () => {
  it('should create an instance', () => {
    expect(new CrossBrowserResult()).toBeTruthy();
  });
});
